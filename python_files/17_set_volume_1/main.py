from microbit import *
import music

# --- SETUP
# start components

# store variables

# --- RUNNING
while True:
    set_volume(255)
    music.play(music.BADDY)
    set_volume(127)
    music.play(music.BADDY)
    set_volume(0)
    music.play(music.BADDY)